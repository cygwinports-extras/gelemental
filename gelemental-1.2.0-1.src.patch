diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x Makefile.in -x makefile.in -x configure -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/gelemental-1.2.0/libelemental/Makefile.am src/gelemental-1.2.0/libelemental/Makefile.am
--- origsrc/gelemental-1.2.0/libelemental/Makefile.am	2007-09-24 23:43:46.000000000 -0500
+++ src/gelemental-1.2.0/libelemental/Makefile.am	2007-09-30 19:28:17.437500000 -0500
@@ -6,7 +6,7 @@
 AM_CXXFLAGS = $(LIBELEMENTAL_CXXFLAGS)
 
 lib_LTLIBRARIES = libelemental.la
-libelemental_la_LDFLAGS = -version-info 0:0:0
+libelemental_la_LDFLAGS = -version-info 0:0:0 -no-undefined
 
 libelemental_la_SOURCES = \
 	private.hh \
diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x Makefile.in -x makefile.in -x configure -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/gelemental-1.2.0/libelemental/misc/ucompose.cc src/gelemental-1.2.0/libelemental/misc/ucompose.cc
--- origsrc/gelemental-1.2.0/libelemental/misc/ucompose.cc	2007-09-24 23:39:02.000000000 -0500
+++ src/gelemental-1.2.0/libelemental/misc/ucompose.cc	2007-09-30 19:30:28.531250000 -0500
@@ -133,7 +133,7 @@
 			output.insert (pos, rep);
 		}
 
-		os.str (std::wstring ());
+		os.str (ustring ());
 		//os.clear ();
 		++arg_no;
 	}
@@ -141,7 +141,7 @@
 
 
 ustring
-UComposition::do_stringify (const std::wstring& src)
+UComposition::do_stringify (const ustring& src)
 {
 	return Glib::convert (string (reinterpret_cast<const char *> (src.data ()),
 		src.size () * sizeof (wchar_t)), "UTF-8", "WCHAR_T");
diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x Makefile.in -x makefile.in -x configure -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/gelemental-1.2.0/libelemental/misc/ucompose.hh src/gelemental-1.2.0/libelemental/misc/ucompose.hh
--- origsrc/gelemental-1.2.0/libelemental/misc/ucompose.hh	2007-09-24 23:39:02.000000000 -0500
+++ src/gelemental-1.2.0/libelemental/misc/ucompose.hh	2007-09-30 19:31:08.937500000 -0500
@@ -79,11 +79,11 @@
 
 	void do_arg (const ustring &rep);
 
-	ustring do_stringify (const std::wstring& src);
+	ustring do_stringify (const ustring& src);
 
 	template<typename T> inline string stringify (T obj);
 
-	std::wostringstream os;
+	std::ostringstream os;
 	int arg_no;
 
 	//!
diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.egg-info' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x config.rpath -x Makefile.in -x makefile.in -x configure -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/gelemental-1.2.0/src/Makefile.am src/gelemental-1.2.0/src/Makefile.am
--- origsrc/gelemental-1.2.0/src/Makefile.am	2007-09-24 23:49:18.000000000 -0500
+++ src/gelemental-1.2.0/src/Makefile.am	2007-09-30 19:37:48.609375000 -0500
@@ -13,6 +13,6 @@
 	main.hh main.cc
 
 gelemental_LDADD = \
-	$(GELEMENTAL_LIBS) \
+	../libelemental/misc/libmisc-gtk.la \
 	../libelemental/libelemental.la \
-	../libelemental/misc/libmisc-gtk.la
+	$(GELEMENTAL_LIBS)
